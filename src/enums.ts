export enum EDataLayerType {
  BACKGROUND = "background",
  COLLIDERS = "colliders",
  SPAWNPOINTS = "spawnpoints",
  Object = "object",
}

export enum EColliderName {
  PLATFORM = "platform",
  EXIT = "exit",
}
